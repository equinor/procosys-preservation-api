version: "3"

services: 
  api:
    container_name: preservationapi
    image: preservationapi
    build:
      context: .
      dockerfile: Equinor.ProCoSys.Preservation.WebApi/Dockerfile
    ports:
      - "8080:5000"
