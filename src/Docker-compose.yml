version: "3"

services: 
  api:
    container_name: apitestproject
    image: apitestproject
    build:
      context: .
      dockerfile: Equinor.Procosys.Preservation.WebApi/Dockerfile
    ports:
      - "80:80"
      - "443:443"